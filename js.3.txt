.replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]))}
function renderLists(){const sorted=[...expenses].sort((a,b)=>b.date.localeCompare(a.date));document.getElementById('recentList').innerHTML=sorted.slice(0,5).map(rowHTML).join('')||'<div class="empty">Todavía no hay gastos.</div>';renderFullList()}
function renderFullList(){const cat=document.getElementById('filterCategory').value,cur=document.getElementById('filterCurrency').value;let arr=[...expenses].sort((a,b)=>b.date.localeCompare(a.date));if(cat)arr=arr.filter(e=>e.category===cat);if(cur)arr=arr.filter(e=>e.currency===cur);document.getElementById('fullList').innerHTML=arr.map(rowHTML).join('')||'<div class="empty">No hay gastos con esos filtros.</div>';bindRows()}
function bindRows(){document.querySelectorAll('.expense-row').forEach(r=>r.onclick=(ev)=>{if(ev.target.classList.contains('del'))return;const e=expenses.find(x=>x.id==r.dataset.id);openCompare(e.category,e.currency)});document.querySelectorAll('.del').forEach(b=>b.onclick=async(ev)=>{ev.stopPropagation();if(confirm('¿Eliminar este gasto?')){try{if(APP.mode==='cloud'&&APP.user)await cloudDeleteExpense(b.dataset.del);expenses=expenses.filter(e=>e.id!=b.dataset.del);save()}catch(e){alert('No se pudo eliminar el gasto.')}}})}
function renderInsights(){const a=totals(thisMonth,'ARS'),pa=totals(prevMonth,'ARS'),ct=catTotals(thisMonth,'ARS'),top=Object.entries(ct).sort((a,b)=>b[1]-a[1])[0];let arr=[];if(a&&pa)arr.push({t:a>pa?'Gastaste más que el mes pasado':'Gastaste menos que el mes pasado',d:`La diferencia en ARS es de ${money(Math.abs(a-pa))} (${Math.abs((a-pa)/pa*100).toFixed(1)}%).`});if(top&&top[1])arr.push({t:`${top[0]} lidera tus gastos`,d:`Representa ${a?(top[1]/a*100).toFixed(1):0}% del gasto en pesos de este mes.`});const rec=expenses.filter(e=>monthKey(e.date)===thisMonth&&e.recurring==='si'&&e.currency==='ARS').reduce((s,e)=>s+Number(e.amount),0);if(rec)arr.push({t:'Gastos recurrentes',d:`Tenés ${money(rec)} registrados como recurrentes este mes.`});if(!arr.length)arr.push({t:'Todavía faltan datos',d:'Cargá algunos gastos para generar análisis útiles.'});document.getElementById('quickInsights').innerHTML=arr.map(x=>`<div class="insight"><strong>${x.t}</strong><span>${x.d}</span></div>`).join('')}
function sizeCanvas(canvas){const r=canvas.getBoundingClientRect(),d=window.devicePixelRatio||1;canvas.width=Math.round(r.width*d);canvas.height=Math.round(r.height*d);const ctx=canvas.getContext('2d');ctx.setTransform(d,0,0,d,0,0);return {ctx,w:r.width,h:r.height}}
const palette=['#5f8f92','#6f91ab','#b88f8e','#b9a277','#8f91ad','#86a59a','#7fa4b2','#a79a92','#7f9189'];
function drawPie(canvas){const {ctx,w,h}=sizeCanvas(canvas);ctx.clearRect(0,0,w,h);const data=catTotals(thisMonth,'ARS'),vals=Object.entries(data).filter(x=>x[1]>0),sum=vals.reduce((s,x)=>s+x[1],0);if(!sum){ctx.fillStyle='#7c7b74';ctx.fillText('Sin datos',w/2-25,h/2);return}const cx=w*.34,cy=h*.5,r=Math.min(w,h)*.28;let a=-Math.PI/2;vals.forEach(([cat,v],i)=>{const da=v/sum*Math.PI*2;ctx.beginPath();ctx.moveTo(cx,cy);ctx.arc(cx,cy,r,a,a+da);ctx.closePath();ctx.fillStyle=palette[i%palette.length];ctx.fill();a+=da});let y=28;ctx.font='12px system-ui';vals.slice(0,7).forEach(([cat,v],i)=>{ctx.fillStyle=palette[i%palette.length];ctx.fillRect(w*.69,y-9,10,10);ctx.fillStyle='#2f302d';ctx.fillText(`${cat} · ${(v/sum*100).toFixed(0)}%`,w*.69+16,y);y+=24})}
function drawLine(canvas){const {ctx,w,h}=sizeCanvas(canvas);ctx.clearRect(0,0,w,h);const months=['2026-04','2026-05','2026-06','2026-07','2026-08'];const data=months.map(m=>totals(m,'ARS'));const max=Math.max(...data,1),pad=34;ctx.strokeStyle='#d7cfc5';ctx.lineWidth=1;for(let i=0;i<4;i++){let y=pad+(h-pad*2)*i/3;ctx.beginPath();ctx.moveTo(pad,y);ctx.lineTo(w-pad,y);ctx.stroke()}ctx.strokeStyle='#6f91ab';ctx.lineWidth=3;ctx.beginPath();data.forEach((v,i)=>{let x=pad+(w-pad*2)*i/(data.length-1),y=h-pad-(v/max)*(h-pad*2);i?ctx.lineTo(x,y):ctx.moveTo(x,y);ctx.fillStyle='#5f8f92';ctx.beginPath();ctx.arc(x,y,4,0,Math.PI*2);ctx.fill()});ctx.stroke();ctx.fillStyle='#7c7b74';ctx.font='11px system-ui';months.forEach((m,i)=>{let x=pad+(w-pad*2)*i/(months.length-1);ctx.fillText(m.slice(5),x-7,h-10)})}
function drawMain(){chartMode==='pie'?drawPie(document.getElementById('mainChart')):drawLine(document.getElementById('mainChart'))}
document.querySelectorAll('[data-chart]').forEach(b=>b.onclick=()=>{chartMode=b.dataset.chart;document.querySelectorAll('[data-chart]').forEach(x=>x.classList.toggle('active',x===b));drawMain()});
function openCompare(cat,currency){document.getElementById('modalTitle').textContent=cat;document.getElementById('modalSub').textContent=`Comparación ${prevMonth} vs ${thisMonth} · ${currency}`;document.getElementById('modalWrap').classList.add('open');requestAnimationFrame(()=>drawCompare(cat,currency))}
function drawCompare(cat,currency){const {ctx,w,h}=sizeCanvas(document.getElementById('modalChart'));ctx.clearRect(0,0,w,h);const a=catTotals(prevMonth,currency)[cat]||0,b=catTotals(thisMonth,currency)[cat]||0,max=Math.max(a,b,1),base=h-42,bw=Math.min(110,w*.22),xs=[w*.34,w*.66],vals=[a,b],labels=['Mes anterior','Mes actual'];vals.forEach((v,i)=>{const bh=(h-90)*v/max;ctx.fillStyle=i===0?'#b88f8e':'#6f91ab';ctx.fillRect(xs[i]-bw/2,base-bh,bw,bh);ctx.fillStyle='#2f302d';ctx.font='700 13px system-ui';ctx.textAlign='center';ctx.fillText(money(v,currency),xs[i],base-bh-10);ctx.font='12px system-ui';ctx.fillStyle='#7c7b74';ctx.fillText(labels[i],xs[i],base+20)});ctx.textAlign='start'}
document.getElementById('modalClose').onclick=()=>document.getElementById('modalWrap').classList.remove('open');document.getElementById('modalWrap').onclick=e=>{if(e.target.id==='modalWrap')e.currentTarget.classList.remove('open')};
function renderCats(){document.getElementById('filterCategory').innerHTML='<option value="">Todas las categorías</option>'+CATS.map(c=>`<option>${c}</option>`).join('')}
document.getElementById('filterCategory').onchange=