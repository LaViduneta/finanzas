renderFullList;document.getElementById('filterCurrency').onchange=renderFullList;
document.getElementById('description').addEventListener('input',e=>{document.getElementById('autoHint').textContent=`Categoría sugerida: ${categoryFor(e.target.value)}`});
document.getElementById('expenseForm').onsubmit=async e=>{e.preventDefault();const desc=document.getElementById('description').value.trim();const chosen=document.getElementById('category').value;let item={id:Date.now(),amount:Number(document.getElementById('amount').value),currency:document.getElementById('currency').value,description:desc,date:document.getElementById('date').value,payment:document.getElementById('payment').value,category:chosen==='AUTO'?categoryFor(desc):chosen,recurring:document.getElementById('recurring').value,note:document.getElementById('note').value.trim()};try{if(APP.mode==='cloud'&&APP.user){const d=await cloudInsertExpense(item);item.id=d.id;}expenses.push(item);save();e.target.reset();document.getElementById('date').value=new Date().toISOString().slice(0,10);alert('Gasto guardado correctamente.');showView('home')}catch(err){console.error(err);alert('No se pudo guardar el gasto en la nube.')}}
function parseQuick(s){let amount=null;const nums=s.match(/\d+[\d\.,]*/g);if(nums){let raw=nums[0].replace(/\./g,'').replace(',','.');amount=parseFloat(raw);if(/\b(luca|lucas|mil)\b/i.test(s)&&amount<1000)amount*=1000}let pay=['Visa','Amex','Débito','Efectivo','Mercado Pago','Transferencia'].find(x=>s.toLowerCase().includes(x.toLowerCase()))||'Otro';let cur=/\b(dolar|dólar|usd|us\$)\b/i.test(s)?'USD':'ARS';return {amount,category:categoryFor(s),payment:pay,currency:cur,description:s.replace(/gast[eé]\s*/i,'').trim()}}
function addBubble(text,type){const d=document.createElement('div');d.className='bubble '+type;d.textContent=text;document.getElementById('chatMessages').appendChild(d);d.scrollIntoView({behavior:'smooth',block:'end'})}
function quickChat(){const s=document.getElementById('chatInput').value.trim();if(!s)return;addBubble(s,'user');document.getElementById('chatInput').value='';const p=parseQuick(s);if(!p.amount){addBubble('No pude detectar el monto. Probá algo como “gasté 24.500 en Carrefour con Visa”.','ai');return}document.getElementById('amount').value=p.amount;document.getElementById('currency').value=p.currency;document.getElementById('description').value=p.description;document.getElementById('payment').value=p.payment;document.getElementById('category').value=p.category;document.getElementById('date').value=new Date().toISOString().slice(0,10);addBubble(`Interpreté ${money(p.amount,p.currency)} · ${p.category} · ${p.payment}. Ya completé el formulario para que lo revises y guardes.`,'ai')}
document.getElementById('chatSend').onclick=quickChat;document.getElementById('chatInput').onkeydown=e=>{if(e.key==='Enter')quickChat()};
document.getElementById('exportBtn').onclick=()=>{const headers=['fecha','descripcion','categoria','medio','moneda','monto','recurrente','nota'];const rows=expenses.map(e=>[e.date,e.description,e.category,e.payment,e.currency,e.amount,e.recurring,e.note].map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(','));const blob=new Blob([[headers.join(','),...rows].join('\n')],{type:'text/csv;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='mis-finanzas.csv';a.click();URL.revokeObjectURL(a.href)};
function renderAll(){renderKpis();renderLists();renderInsights();renderCats();drawMain()}
window.addEventListener('resize',()=>{drawMain();if(document.getElementById('modalWrap').classList.contains('open')){}});document.getElementById('googleLoginBtn').onclick=googleLogin;
document.getElementById('continueLocalBtn').onclick=()=>{localStorage.setItem('mfContinueLocal','1');document.getElementById('loginScreen').classList.remove('open')};
document.getElementById('accountBtn').onclick=()=>showView('config');
document.getElementById('loginLogoutBtn').onclick=()=>APP.user?logout():googleLogin();
document.querySelectorAll('.gmail-connect').forEach(b=>b.onclick=()=>startGmailConnect(b.dataset.source));
document.getElementById('syncMailBtn').onclick=syncGmail;
document.getElementById('date').value=new Date().toISOString().slice(0,10);renderAll();initBackend();
